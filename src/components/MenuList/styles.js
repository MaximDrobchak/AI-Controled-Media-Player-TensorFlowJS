import { makeStyles } from '@material-ui/core/styles';
export const useStyles = makeStyles({
  menuAccount: {
    position: 'absolute',
    display: 'inline-block',
    width: 230,
    right: 10,
    top: 50,
  },
});
