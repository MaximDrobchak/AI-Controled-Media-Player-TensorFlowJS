import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  root: {
    padding: 50,
  },
  inputFile: {
    width: 300,
    maxWidth: 300,
    minWidth: 300,
  },
  contentImg: {
    height: 100,
  },
});
