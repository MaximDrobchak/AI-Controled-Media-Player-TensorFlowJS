import styled from 'styled-components';
import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(theme => ({
  tagButton: { position: 'fixed', top: 80, right: 0, cursor: 'pointer' },
}));
