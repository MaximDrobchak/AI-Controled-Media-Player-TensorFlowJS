import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  resultButtons: {
    display: "inline-flex",
    width: "70%"
  },
  tagButtonStyle: {
    margin: 10
  }
});
